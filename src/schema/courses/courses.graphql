
  type Query {
      courses: [Course]
      course(id: Int!): Course
  }

  type Course {
    id: Int!,
    title: String!,
    author: String!,
    description: String!,
    topic: String!,
    url: String!
  }
